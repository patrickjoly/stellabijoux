<%= simple_form_for @product, :html => {:multipart => true}  do |f| %>

    <%= f.input :name %>
    <%= f.input :description %>

    <%= f.input :image, :as => :file  %>
    <%= f.input :price %>


    <%= f.association :category %>

    <%= f.button :submit %>
<% end %>
